<%= form_with(model: where_from, local: true) do |form| %>
  <% if where_from.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(where_from.errors.count, "error") %> prohibited this where_from from being saved:</h2>

      <ul>
      <% where_from.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :where_from_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
